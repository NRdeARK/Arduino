#define r1 12
#define r2 11
#define r3 10
void setup() {
  // put your setup code here, to run once:
    pinMode(r3,OUTPUT);
    pinMode(r1,OUTPUT);
    digitalWrite(r1,1);
    pinMode(r2,OUTPUT);
    digitalWrite(r2,0);
}

void loop() {
  // put your main code here, to run repeatedly:

}
